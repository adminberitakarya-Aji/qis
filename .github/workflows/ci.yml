name: CI

on:
  push:
    branches: [master, main]
  pull_request:
    branches: [master, main]

jobs:
  lint-typecheck-test-build:
    name: Lint, Typecheck, Test & Build
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          run_install: false

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'pnpm'

      - name: Get pnpm store directory
        id: pnpm-cache
        run: |
          echo "STORE_PATH=$(pnpm store path)" >> $GITHUB_OUTPUT

      - name: Setup pnpm cache
        uses: actions/cache@v4
        with:
          path: ${{ steps.pnpm-cache.outputs.STORE_PATH }}
          key: ${{ runner.os }}-pnpm-store-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-store-

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Generate Prisma Client
        # Without this, @prisma/client's generated types may be incomplete
        # during lint/typecheck (the model types come from this generation
        # step, not from source). ESLint's typed rules in particular need
        # accurate types to avoid false positives/negatives — an incomplete
        # client can make legitimate code (e.g. a necessary `as any` cast
        # bridging a raw DB string to a narrower union type) look like an
        # "unnecessary" assertion, or vice versa. This only needs the
        # schema file, not a live database connection.
        run: pnpm --filter @qis/api exec prisma generate

      - name: Run lint
        run: pnpm lint

      - name: Run typecheck
        run: pnpm typecheck

      - name: Run tests
        run: pnpm test

      - name: Build all packages
        run: pnpm build

      - name: Run pnpm audit (non-blocking)
        run: pnpm audit --json > audit-report.json || true

      - name: Upload audit report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: pnpm-audit-report
          path: audit-report.json
          retention-days: 7

      - name: Summary
        if: always()
        run: |
          echo "## CI Results" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ Lint passed" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ Typecheck passed" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ Tests passed" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ Build passed" >> $GITHUB_STEP_SUMMARY
          if [ -f audit-report.json ]; then
            VULN_COUNT=$(cat audit-report.json | jq -r '.advisories | length' 2>/dev/null || echo "0")
            echo "- ⚠️ Audit: $VULN_COUNT vulnerabilities found (see artifact)" >> $GITHUB_STEP_SUMMARY
          fi